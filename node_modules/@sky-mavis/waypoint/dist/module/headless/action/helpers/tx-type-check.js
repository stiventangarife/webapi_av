import { SupportedTransaction } from '../send-transaction/common.js';

const isEIP1559CompatibleTransaction = type => type === SupportedTransaction.EIP1559 || type === SupportedTransaction.RoninGasSponsor;
const isRoninGasSponsorTransaction = type => type === SupportedTransaction.RoninGasSponsor;
const isRoninLegacyTransaction = type => type === SupportedTransaction.Legacy;
const isSupportedTransaction = type => type !== undefined && Object.values(SupportedTransaction).includes(type);

export { isEIP1559CompatibleTransaction, isRoninGasSponsorTransaction, isRoninLegacyTransaction, isSupportedTransaction };
