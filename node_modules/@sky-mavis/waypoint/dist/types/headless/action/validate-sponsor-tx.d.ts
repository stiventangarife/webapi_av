import { Address } from "viem";
import { ChainParams, TransactionParams } from "./send-transaction/common";
export type ValidateSponsorTransactionParams = {
    httpUrl: string;
    waypointToken: string;
    transaction: TransactionParams;
    chain: ChainParams;
    currentAddress?: Address;
};
export type ValidateSponsorTransactionResult = {
    payer: {
        address: string;
    };
    hasSponsorQuotaApplied: boolean;
    sponsorQuota?: number;
};
export declare const validateSponsorTransaction: (params: ValidateSponsorTransactionParams) => Promise<ValidateSponsorTransactionResult>;
