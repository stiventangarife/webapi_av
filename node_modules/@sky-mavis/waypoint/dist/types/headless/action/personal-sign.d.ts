import { type Hex, type SignableMessage } from "viem";
export type PersonalSignParams = {
    message: SignableMessage;
    waypointToken: string;
    clientShard: string;
    wasmUrl: string;
    wsUrl: string;
};
export declare const personalSign: (params: PersonalSignParams) => Promise<Hex>;
