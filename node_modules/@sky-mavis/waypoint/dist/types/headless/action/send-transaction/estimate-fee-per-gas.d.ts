import { Client, Hex } from "viem";
import { RequestRoute } from "../helpers/request/types";
import { SupportedTransactionType } from "./common";
export declare const GAS_SUGGESTION_ROUTES: Record<number, RequestRoute>;
export declare const GAS_PRICE_BUFFER_PERCENTAGE = 2;
export declare const applyBuffer: (value: bigint, percentage: number) => bigint;
export interface GasSuggestionResponse {
    base_fee_per_gas: bigint;
    exact_base_fee: bigint;
    low: GasPriceLevel;
    medium: GasPriceLevel;
    high: GasPriceLevel;
}
export interface GasPriceLevel {
    max_priority_fee_per_gas: bigint;
    max_fee_per_gas: bigint;
}
export interface EstimateFeesPerGasReturnType {
    gasPrice: Hex;
    maxFeePerGas: Hex;
    maxPriorityFeePerGas: Hex;
}
export interface EstimateFeesPerGasParams {
    chainId: number;
    type: SupportedTransactionType;
    gasPrice?: Hex;
    maxFeePerGas?: Hex;
    maxPriorityFeePerGas?: Hex;
}
export declare function estimateFeesPerGas(client: Client, params: EstimateFeesPerGasParams): Promise<EstimateFeesPerGasReturnType>;
