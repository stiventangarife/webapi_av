import type { ChainParams, TransactionInServerFormat } from "./common";
export declare const sendTransactionRequest: (socket: WebSocket, txData: TransactionInServerFormat, chain: ChainParams) => void;
