import { Frame } from "../../proto/rpc";
export declare const openSocket: (url: string, timeout?: number) => Promise<WebSocket>;
export declare const createFrameQueue: (socket: WebSocket) => {
    waitAndDequeue: (timeout?: number) => Promise<Frame>;
    messageQueue: Uint8Array[];
};
