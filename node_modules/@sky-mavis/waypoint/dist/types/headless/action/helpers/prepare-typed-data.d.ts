import { type HashTypedDataParameters, type HashTypedDataReturnType, type TypedData } from "viem";
type MessageTypeProperty = {
    name: string;
    type: string;
};
export declare function prepareTypedData<const typedData extends TypedData | Record<string, unknown>, primaryType extends keyof typedData | "EIP712Domain">(parameters: HashTypedDataParameters<typedData, primaryType>): HashTypedDataReturnType;
export declare function hashStruct({ data, primaryType, types, }: {
    data: Record<string, unknown>;
    primaryType: string;
    types: Record<string, readonly MessageTypeProperty[]>;
}): `0x${string}`;
export declare function encodeType({ primaryType, types, }: {
    primaryType: string;
    types: Record<string, readonly MessageTypeProperty[]>;
}): string;
export {};
