import type { RequestOption, RequestRoute, ResponseType } from "./types";
export declare const request: <R = unknown, E = unknown>(route: RequestRoute, option?: RequestOption) => Promise<ResponseType<R, E>>;
