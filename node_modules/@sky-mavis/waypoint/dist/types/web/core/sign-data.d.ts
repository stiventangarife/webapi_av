import { type Address, type TypedDataDefinition } from "viem";
import { CommunicateHelper } from "../../common/communicate";
import { WaypointConfig } from "../common";
type SignTypedDataV4Params = {
    config: WaypointConfig;
    params: [address: Address, data: TypedDataDefinition | string];
    expectAddress?: Address;
    clientId: string;
    chainId: number;
    waypointOrigin: string;
    communicateHelper: CommunicateHelper;
};
export declare const signTypedDataV4: ({ config, params, expectAddress, clientId, chainId, waypointOrigin, communicateHelper, }: SignTypedDataV4Params) => Promise<`0x${string}`>;
export {};
