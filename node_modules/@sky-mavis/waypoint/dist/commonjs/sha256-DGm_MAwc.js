'use strict';

var sha256$1 = require('./sha256-CLylCSfy.js');
var size = require('./size-BDN9jZuU.js');
var toBytes = require('./toBytes-Dmnw03lt.js');

function sha256(value, to_) {
  const to = to_ || 'hex';
  const bytes = sha256$1.sha256(size.isHex(value, {
    strict: false
  }) ? toBytes.toBytes(value) : value);
  if (to === 'bytes') return bytes;
  return toBytes.toHex(bytes);
}

exports.sha256 = sha256;
